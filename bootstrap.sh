# MyoTwin Monorepo Bootstrap Script

Install melos and bootstrap the monorepo:

```bash
dart pub global activate melos
melos bootstrap
```

Each package can then be developed independently with `dart pub get` or `flutter pub get`.
