name: myotwin_ui
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

resolution: workspace

environment:
  sdk: ">=3.11.5"
  flutter: ">=3.41.9"

dependencies:
  flutter:
    sdk: flutter
  flutter_shaders: ^0.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  shaders:
    - assets/shaders/holo_glitch.frag
  uses-material-design: true
